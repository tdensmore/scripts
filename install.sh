
# GIT
sudo apt-get install git

# PYTHON
sudo apt-get install python
sudo apt-get install python-pip

# BOTO
pip install boto

# AWS CLI
# Pip installs the aws executable to /usr/bin/aws.
# The awscli library (which does the actual work) is installed
# to the 'site-packages' folder in Python's installation directory.
sudo pip install awscli
sudo pip install --upgrade awscli

